# AI FUNCTIONAL SPEC (APPEND-ONLY)

ВНИМАНИЕ: Этот файл append-only.

- Нельзя удалять или переформулировать ранее зафиксированные инварианты.
- Допускается только добавление новых правил и блоков `Изменено на...`.

## 1. Системные инварианты

1. Каждая операция обязана работать в контексте владельца `telegram_user_id`.
2. Нельзя выдавать или изменять сущности другого пользователя.
3. Все callback значения должны валидироваться по формату и принадлежности.
4. Ошибки внешних сервисов не должны приводить к потере записи дневника.

## 2. Контракты сущностей

### 2.1 Habit
- Поля: `id`, `user_id`, `name`, `habit_type`, `is_active`, `created_at`.
- `habit_type`: `positive|negative`.

### 2.2 HabitScheduleRule
- Поля: `habit_id`, `schedule_type`, `time_slot`, `weekday`, `interval_days`, `start_from`.
- `schedule_type`: `daily|every_other_day|specific_weekdays`.
- `time_slot`: `morning|day|evening`.

### 2.3 HabitCheckin
- Уникальность: `(habit_id, check_date, time_slot)`.
- `status`: `done|missed|violated|optional_done`.

### 2.4 DiaryEntry
- Поля: `entry_date`, `entry_type`, `text_body`, `tags`, `created_at`.
- `entry_type`: `text|voice|mixed`.

### 2.5 DiaryVoice
- Поля: `entry_id`, `telegram_file_id`, `telegram_file_unique_id`, `duration_sec`, `mime`, `message_id`.

### 2.6 DiaryTranscript
- Поля: `entry_id`, `transcript_text`, `language`, `confidence`, `stt_status`, `attempts`, `last_error`, `updated_at`.
- `stt_status`: `pending|done|failed`.

## 3. Алгоритмы

### 3.1 Плановые и сверхплановые метрики
- Плановые слоты считаются только в невыходные дни.
- Сверхплан — успешные слоты в выходные.
- Формулы:
  - `plan_completion = completed_slots / plan_slots * 100`
  - `over_completion = (completed_slots + extra_slots) / plan_slots * 100`

### 3.2 Негативные привычки
- Явный срыв = `violated`.
- Для прошедшей даты отсутствие срыва может интерпретироваться как успех при расчетах.

### 3.3 Транскрибация голосовых
- Голосовая запись сохраняется до попыток STT.
- STT выполняется с ретраями.
- При неуспехе выставляется `failed`, запись дневника не теряется.

### 3.4 Поиск заметок
- База поиска: `DiaryEntry.text_body` + `DiaryTranscript.transcript_text`.
- Используются FTS и trigram индексы Postgres.
- При режиме `voice_only` возвращаются только записи, имеющие `DiaryVoice`.

## 4. Контракты команд/экранов

- `/start` — приветствие и список команд.
- `/today` — привычки ближайшего слота.
- `/habits` — inline CRUD привычек.
- `/calendar` — текущая неделя и навигация.
- `/diary` — добавление текст/голос и список заметок дня.
- `/search_notes` — отдельный поиск заметок.
- `/stats` — агрегированная статистика.
- `/export_diary` — ZIP markdown+media.
- `/export_stats_html` — статический HTML отчет.

## 5. Callback namespace

- `habits:*` — привычки и отметки.
- `calendar:*` — календарь.
- `diary:*` — дневник.
- `search:*` — поиск заметок.
- `stats:*` — выбор периода статистики.

## 6. Edge-cases

1. Пустое название привычки отклоняется.
2. Пустой поисковый запрос отклоняется.
3. Отмена отметки без истории должна вернуть мягкую ошибку.
4. Неуспешная транскрибация не должна блокировать экспорт.
5. Экспорт без данных возвращает пустой, но валидный архив/отчет.

## 7. Политика изменения функций

Для любой измененной функции добавлять рядом запись формата:

`Изменено на: <новое поведение>. Причина: <почему>. Коммит: <hash>.`

Пример для будущих правок:
- Изменено на: расширен parser callback для новых состояний. Причина: добавлен новый экран. Коммит: abc1234.

## История изменений (append-only)

- Изменено на: добавлен namespace `settings:*` для выбора/сохранения weekday day-off правил. Причина: управление множественными выходными через UI. Коммит: abc0c6f.
- Изменено на: добавлен callback namespace `main:open:*` для inline-навигации между разделами. Причина: главный интерфейс должен быть inline-first. Коммит: abc0c6f.
- Изменено на: в `0001_base_habits` enum-объекты переведены на `create_type=False` при ручном `create(checkfirst=True)`. Причина: устранение `DuplicateObject` на `alembic upgrade head`. Коммит: 0cf872d.
- Изменено на: в миграции `0001_base_habits` enum-объекты заменены на `sqlalchemy.dialects.postgresql.ENUM(..., create_type=False)`. Причина: `sa.Enum` продолжал создавать типы при `CREATE TABLE`. Коммит: 3681110.
- Изменено на: `0002_diary_entries` использует PostgreSQL-тип enum c отключенным автосозданием на `CREATE TABLE`. Причина: предотвращение `DuplicateObject` для `diary_entry_type`. Коммит: cd11194.
- Изменено на: добавлены callback-контракты `main:submenu:*` для иерархической навигации (root/analytics/more). Причина: сокращение кнопок главного меню. Коммит: 86bbc4d.
- Изменено на: удалены command-only входы в разделы (кроме стартовой точки), основной доступ реализован callback-first. Причина: требование button-only интерфейса. Коммит: 86bbc4d.
- Изменено на: `_render_*` функции перестали подтверждать callback самостоятельно, подтверждение переносится в вызывающие callback-обработчики. Причина: исключить поздний ACK и ускорить UX. Коммит: 86bbc4d.
- Изменено на: календарь недели использует агрегированный расчет индикаторов за один проход вместо множественных сессий на день. Причина: уменьшить latency. Коммит: 86bbc4d.
- Изменено на: для сущности пользователя `telegram_user_id` расширен до `BIGINT` в модели и миграциях. Причина: предотвратить переполнение `int32` и падения callback-сценариев для больших Telegram ID. Коммит: 4e8c5b9.
- Изменено на: command-фильтр старта удален; открытие главного меню выполняется сообщением в `State=None`, что сохраняет button-only UX без обязательных slash-команд. Причина: убрать командный интерфейс и оставить только кнопочный поток. Коммит: 4e8c5b9.
- Изменено на: `TimeSlot` расширен значением `all_day`, а фильтрация `is_rule_due` считает `all_day`-правила релевантными для экранов `утро/день/вечер` и отдельного экрана `весь день`. Причина: поддержать привычки без привязки к конкретному часу. Коммит: aae24cd.
- Изменено на: добавлены callback-контракты `settings:menu`, `settings:open:*` как единый вход в календарь/поиск/экспорт/статистику/выходные. Причина: перенести вторичные функции в глобальный раздел `Настройки`. Коммит: aae24cd.
- Изменено на: добавлены callback-контракты `habits:tap:{habit_id}:{action_slot}:{selected_slot}` и `habits:fail:{habit_id}:{action_slot}:{selected_slot}` для one-tap отметок по привычке. Причина: по нажатию на привычку должна сразу ставиться отметка выполнения. Коммит: aae24cd.
- Изменено на: исправлен парсинг legacy callback `habits:checkin:*` (правильная арность), чтобы исключить падение обработчика при отметке. Причина: устранение runtime-краша в checkin flow. Коммит: aae24cd.
- Изменено на: SQLAlchemy enum-маппинг переведен на хранение `.value` через `values_callable`, чтобы БД получала значения в нижнем регистре (`positive/negative/...`). Причина: устранение `InvalidTextRepresentationError` при записи enum в PostgreSQL. Коммит: 8f01757.
- Изменено на: callback-контракты рутины привычек разделены на рабочие (`habits:menu:*`, `habits:tap:*`, `habits:fail:*`) и сервисные (`habits:manage`, `habits:add:*`, `habits:edit:*`, `habits:delete:*`) с доступом к сервисным из `settings`. Причина: разгрузить основной экран рутины. Коммит: 2dd8903.
- Изменено на: `HabitCreateInput` и модель `Habit` расширены полем `icon_emoji`, создается новый шаг FSM `HabitStates.waiting_icon`. Причина: персональная emoji-метка привычки обязательна для быстрого визуального распознавания. Коммит: 2dd8903.
- Изменено на: `export_diary_message`/`export_stats_html_message` принимают явный `telegram_user_id` и в callback-сценариях используют `callback.from_user.id`. Причина: устранить ошибочный экспорт данных пользователя бота. Коммит: 2dd8903.
- Изменено на: day-off UI переведен на exact-date toggles с контрактами `settings:dayoff:view|shift|toggle` для режимов `week|month`. Причина: плавающие выходные по датам. Коммит: 2dd8903.
- Изменено на: добавлен weekly feedback flow с таблицей `weekly_prompts`, фоновым джобом `weekly_digest_loop`, callback `weekly:comment:{week_start}` и FSM `WeeklyStates.waiting_weekly_comment`. Причина: автоматический weekly review с сохранением комментария в дневник. Коммит: 2dd8903.
- Изменено на: revision id миграции weekly-блока укорочен до `0007_habit_icon_weekly` (<=32 символов). Причина: `alembic_version.version_num` не принимал длинный идентификатор и upgrade падал на финальном шаге. Коммит: 8f41fd1.
- Изменено на: добавлен утилитарный слой `safe_edit_text`/`safe_edit_reply_markup` с игнорированием `TelegramBadRequest: message is not modified`. Причина: повторные callback-нажатия не должны ломать обработчик. Коммит: c0e2e51.
- Изменено на: введен фоновый воркер `transcription_worker_loop` с in-memory очередью и восстановлением pending-транскрипций при старте. Причина: убрать блокирующую STT-работу из message-handlers и повысить отзывчивость бота. Коммит: c0e2e51.
- Изменено на: `diary` и `weekly` voice handlers переводят транскрибацию в очередь (`enqueue_transcription`) вместо inline STT-цикла. Причина: предсказуемое время ответа на отправку длинных ГС. Коммит: c0e2e51.
- Изменено на: сущности `Habit`/`HabitCheckin` расширены sport-полями (`is_sport`, baseline/step, target snapshot, actual reps csv) и добавлена миграция `0008_sport_progress`. Причина: хранить линейный спорт-план и фактический результат без разрушения базовой модели привычек. Коммит: a45bb51.
- Изменено на: добавлен доменный расчет `compute_linear_target(habit, date)` для линейной прогрессии по неделям. Причина: единый инвариант вычисления целевого значения в UI и checkin flow. Коммит: a45bb51.
- Изменено на: в callback `habits:tap:*` для sport привычек запускается FSM `waiting_sport_result` с обязательным вводом факта по подходам; затем checkin записывается с target/actual полями. Причина: учитывать неполное выполнение подходов внутри одной тренировки. Коммит: a45bb51.
- Изменено на: flow создания привычки расширен шагами спорт-настройки (`habits:add:sport:*`, `waiting_sport_target`, `waiting_sport_step`). Причина: пользователь задает линейный план прямо при создании спорт-привычки. Коммит: a45bb51.

- Изменено на: контракт Habit расширен полем sport_progression_enabled (bool), а HabitCheckin — полем sport_plan_adhered (nullable bool). Причина: хранить состояние прогрессии и факт соблюдения плана без потери истории. Коммит: 61e6c4b.
- Изменено на: callback-контракт habits:sportadherence:{habit_id}:{slot}:{selected_slot}:{yes|no} фиксирует соблюдение плана прогрессии для спорт-привычки. Причина: заменить текстовый ввод факта на быстрый inline-сценарий. Коммит: 61e6c4b.
- Изменено на: callback-контракты habits:sportcfg:* (open/base/step/toggle) и FSM-состояния waiting_sport_update_target, waiting_sport_update_step добавлены для редактирования спорт-параметров из настроек. Причина: централизовать редактирование спорт-настроек в сервисном меню. Коммит: 61e6c4b.
- Изменено на: compute_linear_target учитывает sport_progression_enabled; при False всегда возвращает базовые значения. Причина: поддержка режима фиксированной нагрузки без линейного роста. Коммит: 61e6c4b.
- Изменено на: результат метрик периода расширен полями pullups_reps и pushups_reps; объем считается по успешным спорт-чекинам с приоритетом ctual_reps_csv, затем 	arget_sets*target_reps. Причина: отдельная аналитика силовой нагрузки по упражнениям. Коммит: 61e6c4b.
- Изменено на: повторная фиксация инварианта расчета sport-объема — приоритет `actual_reps_csv`, затем `target_sets*target_reps`, затем fallback на `compute_linear_target`.
  Причина: устранить неоднозначность из-за поврежденной строки кодировки в предыдущей записи append-only.
  Коммит: 61e6c4b.
- Изменено на: callback-контракт `habits:sportcfg:preset:{habit_id}:{sets}:{reps}` применяет быстрый шаблон базового плана и перезапускает отсчет прогрессии от текущей даты. Причина: быстрый one-tap старт уровня для подтягиваний/отжиманий. Коммит: 977b6bd.
- Изменено на: callback-контракт `habits:menu:{auto|all}` заменил множественные переключатели слотов; в режиме `auto` слот выбирается по текущему часу. Причина: упрощение рабочей навигации по привычкам. Коммит: 9dc6707.
- Изменено на: callback `habits:tap:{habit_id}:{slot}:{view_mode}` стал toggle-операцией (поставить отметку / снять существующую). Причина: отмена действия повторным тапом без отдельной кнопки fail. Коммит: 9dc6707.
- Изменено на: для `Habit` добавлены поля цели `goal_days`, `goal_start_date`, `goal_completed_cycles`, миграция `0010_habit_goal_cycles`. Причина: хранить циклические цели привычек в доменной модели. Коммит: 9dc6707.
- Изменено на: добавлены callback-контракты `habits:goalcfg:*` и FSM-состояние `HabitStates.waiting_goal_days` для настройки/продления/сброса целей через `Настройки`. Причина: управление целями без перегруза рабочего экрана. Коммит: 9dc6707.
- Изменено на: сервис метрик расширен методом `compute_habit_progress(...)`, возвращающим по каждой привычке weekly success/due, текущую серию, общий счет дней соблюдения и прогресс цели. Причина: поддержать персонализированную недельную аналитику по привычкам. Коммит: 9dc6707.
- Изменено на: callback-навигация `main:open:*`, `diary:*`, `settings:*`, `search:*`, `calendar:*` в основных экранах теперь использует `safe_edit_text(...)` вместо `message.answer(...)` для переходов. Причина: гарантировать single-message inline UX и устранить каскад новых сообщений. Коммит: 489a60a.
- Изменено на: семантика `habits:menu:auto` уточнена — если `list_due_habits(..., slot=current_slot)` пуст, используется fallback `list_due_habits(..., slot=None)` с информирующим текстом. Причина: предотвратить «пустой экран» при существующих привычках в других слотах дня. Коммит: 489a60a.
- Изменено на: `_status_marker(None, HabitType.NEGATIVE)` возвращает `✅` как базовое состояние; `habits:tap` для негативной привычки создает `violated`, а повторный тап удаляет checkin. Причина: инвариант «негативная привычка по умолчанию соблюдена, пока не зафиксирован срыв». Коммит: 489a60a.
- Изменено на: `_habit_emoji` больше не выводит авто-маркеры типа (`✅/🚫`) и возвращает только пользовательский `icon_emoji` при наличии. Причина: сократить визуальный шум и исключить дублирование статусных эмодзи. Коммит: 489a60a.
- Изменено на: экран `calendar:day:*` переведен в inline-детализацию с callback `calendar:back:{week_start}` для возврата к недельной сетке в том же сообщении. Причина: соответствие single-message контракту навигации. Коммит: 489a60a.
